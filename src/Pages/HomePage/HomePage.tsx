import HeroSection from "../../Components/HeroSection/HeroSection";
import "./style.scss";

interface IProps {}
export default ({}: IProps) => {
  return (
    <div>
      <HeroSection />
    </div>
  );
};
